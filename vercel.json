{
    "version": 2,
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/app.py" }
    ],
    "builds": [
      {
        "src": "/app.py",
        "use": "@vercel/python"
      }
    ]
  }
  